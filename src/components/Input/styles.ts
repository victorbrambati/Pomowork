import styled from 'styled-components';

export const Container = styled.div`
  width: 164px;
  height: 39px;
  background: var(--secondary);
  border-radius: 16px;

  > input {
    width: 87%;
    height: 100%;
    margin-left: 13%;
    color: var(--tertiary);
    font-size: 16px;
    line-height: 18px;
    outline: none;
  }
`;
